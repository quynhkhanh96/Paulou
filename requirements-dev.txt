# Dev/test dependencies for the stages built so far (build order steps 1-2:
# pure functions, G2P, POS tagging — see Roadmap).
#
# NOT a pip dependency — install separately as a system package:
#   apt install espeak-ng        # Debian/Ubuntu
#   brew install espeak-ng       # macOS
# Used as the eSpeak-ng fallback in paulou/stages/chunk_analyzer/g2p/lexique_espeak.py.
# Tests that need it are skipped automatically if it's missing.

pytest>=8

# POS tagging (paulou/stages/chunk_analyzer/pos/spacy_tagger.py) — plain function, no
# Protocol/registry (Decision Log D26).
spacy>=3.8

# The French model itself is a separate package, not pulled in by `pip
# install spacy` alone. Verified against fr_core_news_sm 3.8.0 (Codebase
# Conventions: pin model-related dependencies) — see SETUP.md for the
# install command.

# Sentence parser (paulou/stages/parsing/gemini_parser.py) — Gemini API,
# not Claude API as the Architecture Spec names (Decision Log D27).
# Requires >=2.3.0 for the Interactions API (client.interactions.create);
# verified against 2.22.0.
google-genai>=2.3.0

# Loads GEMINI_API_KEY from a .env file at the repo root (see .env.example)
# — never hardcoded, never passed through PipelineConfig (Codebase
# Conventions).
python-dotenv>=1.0

# TTS (paulou/stages/tts/) — two swappable implementations (Decision Log
# D12, D31), like Kaldi vs gop-ft for the GOP scorer:
azure-cognitiveservices-speech>=1.51.0   # azure_tts.py — official SDK, needs Azure credentials
edge-tts>=7.2.0                          # edge_tts_provider.py — unofficial, no API key (D31)

# Audio slicing (paulou/stages/tts/audio_slicing.py, Decision Log D5) —
# needs ffmpeg as a SYSTEM dependency (not pip), e.g. `apt install ffmpeg`.
# KNOWN FUTURE RISK: pydub depends on the stdlib `audioop` module, which
# is REMOVED (not just deprecated) in Python 3.13 — pydub has not been
# updated for this as of the version pinned below. Revisit if upgrading
# past Python 3.12.
pydub>=0.25.1

# Heavier stage-specific dependencies (Kaldi, PyTorch, HuggingFace
# transformers) are deferred until those stages are actually built, per
# Codebase Conventions ("Dependency management") — not bundled in up front.